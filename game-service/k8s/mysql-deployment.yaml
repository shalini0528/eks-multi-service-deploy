apiVersion: v1
kind: Service
metadata:
  name: mysql-game-service
  namespace: game
spec:
  selector:
    app: mysql-game-service
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-game-service
  namespace: game
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-game-service
  template:
    metadata:
      labels:
        app: mysql-game-service
    spec:
      containers:
      - name: mysql
        image: mysql:8
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "root"
        - name: MYSQL_DATABASE
          value: "lugxdbGame"
        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-storage
        emptyDir: {}
