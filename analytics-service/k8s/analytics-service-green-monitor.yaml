apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: analytics-service-green
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: analytics-service
      version: green
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
  namespaceSelector:
    matchNames:
      - analytics
